destination:
  server: https://kubernetes.default.svc
argocd-config:
  apps:
    crossplane-dev:
      targetRevision: HEAD
      path: apps/crossplane
      syncPolicy: {
        syncOptions: {}
      }
    kyverno-dev:
      targetRevision: HEAD
      path: apps/kyverno
      syncPolicy: {
        syncOptions: {}
      }

